<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% for (let image of images) { %>
        <img width="300px" src="<%= image.image %>" />
        <form action="<%= `/search/saved/${image.id}?_method=DELETE` %>" method="POST">
          <div class="comment">
            <a href="<%= `/search/saved/${image.id}` %>">Edit?</a>
          </div>
        <input type="submit" value="Delete"/>
        </form>
      <% } %>

    <a href="/">back</a>
  </body>
</html>
